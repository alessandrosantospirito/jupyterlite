{
    "name": "ML-Experiments Devcontainer",
    "build": {
      "dockerfile": "Dockerfile",
      "context": ".."
    },
    "runArgs": [
      "--net=host"
  ],
  "containerEnv": {
      "DISPLAY": "${localEnv:DISPLAY}"
  },
  "forwardPorts": [8888]
}
